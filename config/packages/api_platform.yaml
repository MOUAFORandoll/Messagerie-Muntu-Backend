api_platform:
  mapping:
    paths: ["%kernel.project_dir%/src/Entity"]
  patch_formats:
    json: ["application/merge-patch+json"]
  swagger:
    versions: [3]
    api_keys:
      PAIEMENT_KEY:
        name: Authorization
        type: header
  defaults:
    pagination_enabled: false
